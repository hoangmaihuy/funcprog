(

(define (f1 x) (+ x 1))
(define (f2 x) (+ x 2))
(define (f3 x) ( * 2 x))
(define k (((acc-func f1 f2) f2 f3) f2))
((k) 3)

)

(
(define (f4 x) ( - x 1))
(define kk ((((((acc-func))) f3 f4 ) f2)))
(kk 2)
)
